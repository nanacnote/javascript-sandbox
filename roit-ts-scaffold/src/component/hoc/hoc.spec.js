// import StopPropagation from "./stopPropagation.riot";
import { expect } from "chai";
import { component } from "riot";

// const sp = component(StopPropagation)(document.createElement("div"))

describe("Stop propagation HOC Unit Test", () => {
  it("The component is properly rendered", () => {
    // expect(sp.$$("div")[0]).to.be.true;
  });
});